# Core engine
langgraph>=0.5
langgraph-checkpoint-sqlite~=2.0.10   # durable conversation memory (data/conversations.db)
openai>=1.90            # OpenAI-compatible client (works with Ollama, LM Studio, Groq, OpenAI)
numpy>=1.26

# UI (chat history persistence uses chainlit's SQLAlchemy data layer + aiosqlite)
chainlit>=2.9
aiosqlite>=0.20

# Supabase / PostgreSQL backend (used when APP_DATABASE_URL / DATABASE_URL is set)
psycopg2-binary>=2.9
langgraph-checkpoint-postgres~=2.0   # conversation memory in Supabase on the postgres backend
psycopg[binary]>=3.1                 # driver for the Postgres checkpointer
asyncpg>=0.29                        # async driver for chat history on the postgres backend

# Optional: local speech-to-text (voice input). Comment out if not needed.
faster-whisper>=1.0

# Tests
pytest>=8.0
